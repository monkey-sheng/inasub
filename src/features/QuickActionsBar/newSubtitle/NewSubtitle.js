import React, {Component} from 'react';
import PropTypes from 'prop-types';

/**
 * click it to create a new subtitle file
 */
class NewSubtitle extends Component {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

NewSubtitle.propTypes = {};

export default NewSubtitle;
