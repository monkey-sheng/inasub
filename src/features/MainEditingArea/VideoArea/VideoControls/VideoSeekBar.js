import React, {Component} from 'react';
import PropTypes from 'prop-types';

class VideoSeekBar extends Component {
  render() {
    return (
      <div>
        {/*TODO*/}
      </div>
    );
  }
}

VideoSeekBar.propTypes = {
  vidRef: PropTypes.any
};

export default VideoSeekBar;
