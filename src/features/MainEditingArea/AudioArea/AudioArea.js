import React, {Component} from 'react';
import PropTypes from 'prop-types';


/**
 * Display the waveform and controls, and maybe something else
 */
class AudioArea extends Component {
  render() {
    return (
      <div>

      </div>
    );
  }
}

AudioArea.propTypes = {};

export default AudioArea;
